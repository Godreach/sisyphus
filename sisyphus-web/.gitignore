# 前端依赖（npm ci 按 package-lock.json 安装，无需进 git）
node_modules/

# Vite/vue-tsc 增量缓存
*.tsbuildinfo

# 前端构建产物：本地 npm run build 生成，不提交。rust-embed 要求的目录存在性
# 由 sisyphus-server/build.rs 编译前兜底创建，dist 内不需要任何 git 占位文件
# （vite build 的 emptyOutDir 会清空 dist，占位文件留不住）。本文件位于
# sisyphus-web/，规则相对本目录。
dist/
